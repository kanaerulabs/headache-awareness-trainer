/**
 * Hooks Index
 *
 * Export all Clean Architecture hooks.
 * Note: useHeadacheLogging was removed - use useLoggingStore instead
 * (it has more features: progressive unlocking, metadata, streak calculation)
 */

export { useCheckIn } from "./useCheckIn";
export { useDashboard } from "./useDashboard";
