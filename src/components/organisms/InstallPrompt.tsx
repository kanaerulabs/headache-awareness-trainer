"use client";

/**
 * InstallPrompt - A2HS (Add to Home Screen) prompt component
 *
 * DEPRECATED: This component is no longer used for automatic prompts.
 * Install functionality has been moved to Settings > InstallAppButton.
 * This component is kept for backward compatibility but returns null.
 */
export function InstallPrompt() {
  // Install prompt functionality moved to Settings page (InstallAppButton)
  // This component is disabled to avoid intrusive popups
  return null;
}
